import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  HomeContainer: {
    backgroundColor: 'red',
    paddingHorizontal: 20,
    paddingVertical: 7,
    width: '100%',
    borderRadius: 10,
  },
});
